#include <stdio.h>
#include <stdlib.h>
//Wap to accept a number and display the number of digits in that number.
int main()
{
  int num, digit = 0;

  printf("Enter a number: ");
  scanf("%d", &num);
  while(num>0)
  {
      num = num/10;
      digit++;
  }
  printf("%d", digit);
}
