#include <stdio.h>
#include <stdlib.h>
//Wap to accept a number and display the number in reverse
int main()
{
  int num, rem;

  printf("Enter a number: ");
  scanf("%d", &num);
  while(num>0)
  {
      rem = num%10;
      num = num/10;
      printf("%d", rem);
  }
}
