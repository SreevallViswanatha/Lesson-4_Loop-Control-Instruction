#include <stdio.h>
#include <stdlib.h>
//Wap to accept a number and display its factors
int main()
{
  int num, i;
  printf("Enter a number: ");
  scanf("%d", &num);
  i = 2;
  while(i<=num/2)
  {
      if(num%i==0)
        printf("%d\n", i);
      i++;
  }
}
