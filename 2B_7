#include <stdio.h>
#include <stdlib.h>
//Print a number in reverse
int main()
{
   int num, rem;
   printf("Enter the number: ");
   scanf("%d", &num);

   while(num>0)
   {
       rem = num%10;
       num = num/10;
       printf("%d", rem);
   }

}
